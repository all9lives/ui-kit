import styled from 'styled-components'
import base, { Component } from './base'
import defaultProps from './defaultProps'

const Toggle = styled(Component)`
  ${base}
`
Toggle.defaultProps = defaultProps

export default Toggle
